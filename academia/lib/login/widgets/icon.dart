import 'package:flutter/material.dart';

class IconCar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.directions_car,
      size: 170,
    );
  }
}
